from django.db import models
from django.core.exceptions import ValidationError
# Create your models here.

#class SignUp(models.Model):
    #username = models.CharField(label='Username')
    #email = models.EmailField(label='Email Address')
    #phone = models.CharField(label='Contact Number')
    #password1 = models.CharField(label='Password')
    #password2 = models.CharField(label='Password Confirmation')